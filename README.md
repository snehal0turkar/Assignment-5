# Assignment-5
task1.py takes the input from the user of student name. The code has the dictonary of student name and names as value and student name as key.\
task2.py prints the 1 to 10 numbers then extracts 1st 5 elements and then its reverse list.
